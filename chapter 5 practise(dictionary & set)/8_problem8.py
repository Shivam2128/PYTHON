d ={}

name = input('enter name of friends:')
lang = input('enter language name:')
d.update({name :lang})

name = input('enter name of friends:')
lang = input('enter language name:')
d.update({name :lang})

name = input('enter name of friends:')
lang = input('enter language name:')
d.update({name :lang})

name = input('enter name of friends:')
lang = input('enter language name:')
d.update({name :lang})

print(d)


# OP {'shivam': 'c++', 'harry': 'c++\\', 'rup': 'c++', 'rohit': 'py'} 
# nothing will happen to set beacuse if we change key than only it will effect to  the set 
#and if we change the value and its same so it will not effect
